import { FileBase, FileBaseOptions, IResolver } from './file';
import { Project } from './project';
/**
 * @experimental
 */
export interface TomlFileOptions extends FileBaseOptions {
    /**
     * (experimental) Object to render in the TOML file.
     *
     * @experimental
     */
    readonly obj: any;
}
/**
 * (experimental) TOML file.
 *
 * @experimental
 */
export declare class TomlFile extends FileBase {
    /**
     * @experimental
     */
    protected readonly obj: object;
    /**
     * @experimental
     */
    constructor(project: Project, filePath: string, options: TomlFileOptions);
    /**
     * (experimental) Implemented by derived classes and returns the contents of the file to emit.
     *
     * @experimental
     */
    protected synthesizeContent(resolver: IResolver): string;
}
