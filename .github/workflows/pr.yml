on:
  pull_request:
    branches:    
      - main

name: Build Project

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Setup
      run: npm install

    - name: Build
      run: npm run build
